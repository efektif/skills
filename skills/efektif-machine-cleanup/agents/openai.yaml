display_name: "Machine Cleanup"
short_description: "Use for local cleanup of tools, skills, apps, caches, and config after the user scopes the cleanup; inventory first and default to dry-run."
default_prompt: "Use efektif-machine-cleanup. Use for local cleanup of tools, skills, apps, caches, and config after the user scopes the cleanup; inventory first and default to dry-run. Follow the Steps in SKILL.md, keep one evidence-backed output, and report verification or blockers."
