display_name: "Install Skill Package"
short_description: "Use when installing a skill package safely: validate, dry-run, apply only when intended, then verify target files."
default_prompt: "Use efektif-install-skill-package. Use when installing a skill package safely: validate, dry-run, apply only when intended, then verify target files. Follow the Steps in SKILL.md, keep one evidence-backed output, and report verification or blockers."
