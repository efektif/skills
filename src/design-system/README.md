# Skills Site Design System

- Contract: workspace `DESIGN.md` v0.2.0.
- Upstream reference: `efektif-ui` commit
  `e7363e347233eeab5c030a7fc8f0ba0c316a00d4`,
  `@efektif/tokens` v2.0.0.
- Vendored asset: `theme.css`, copied from
  `packages/tokens/generated/theme.css`.
- License: `../../LICENSES/efektif-ui-MIT.txt`.
- Defaults: light mode, comfortable density, and default tint are declared on
  the document root.

This directory is owned by the Efektif Skills docs site. Refreshes are
deliberate local changes and must preserve the site's visual behavior; the site
must not regain a runtime or package dependency on the upstream design packages.
